This branch is used to branch front end features which are being worked on. 
It will act as a buffer between the new features and the main master branch to
facilitate testing code with the system before the branch.
